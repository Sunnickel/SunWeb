[package]
name = "example_app"
publish = false
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
sunweb = { path = "../sunweb", version = "0.4.0", features = ["templating"] }
log.workspace = true